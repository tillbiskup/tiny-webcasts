# Install dependencies for macOS using Homebrew
# If you haven't installed Homebrew yet, you may type:
#
#    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"
#
# or check the information available at https://brew.sh/

brew install ffmpeg
brew install sox
brew install flac
brew install normalize
brew install imagemagick
brew install pdfinfo