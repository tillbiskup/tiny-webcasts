# Install dependencies for Debian Linux using the apt package manager
#
# Of course, it would be wise to run this script with root permissions.

apt install ffmpeg
apt install sox
apt install flac
apt install normalize
apt install imagemagick
apt install poppler-utils